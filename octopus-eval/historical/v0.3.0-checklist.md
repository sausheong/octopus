# Historical verification checklist — Octopus v0.3.0 evaluation

> This is an archived result for commit `5800888`, not the current working tree.

Subject: `sausheong/octopus` @ `5800888` (v0.3.0). Date: 2026-08-05.
Results and interpretation: `summary.md`.

| # | Check | Method | Result | Pass |
|---|---|---|---|---|
| T1a | Cacheable prompt caches at all (baseline) | 2× identical 4,808-tok prompt direct to litellm | req2 `cache_read=4808` | ✅ |
| T1b | Cache survives routing through Octopus | same, via Octopus | req2 `cache_read=4808` | ✅ |
| T2a | Request bytes forwarded unmodified | mock upstream byte capture | **NO** — request rebuilt | ❌ (finding) |
| T2b | Tool `input_schema.properties` order preserved | `zebra,apple,mango` probe | **alphabetised** to `apple,mango,zebra` | ❌ (finding) |
| T2c | `cache_control` / `ttl` values preserved | probe with 1h + 5m markers | values intact, keys reordered | ✅ |
| T2d | Isolate config-driven vs unconditional mutation | control run at `reasoning:false` | `max_tokens`+`thinking` were config-driven | ✅ |
| T3 | Cache-aware scoring pins to cached model | real scorer, Terra-shaped profiles | 380k: 2/2 stay. 50k: 2/3 stay | ✅ |
| T4 | Proxy latency overhead | n=200 vs local mock, fresh listeners | +0.27 ms p50 / +0.37 ms p95 | ✅ |
| T5a | Ollama reachable as a backend, no code change | `kind: openai` + `:11434/v1` | real inference, correct Anthropic response | ✅ |
| T5b | Oversized request refused (local sole backend) | 40k req vs 32k model | `invalid_request_error` — fails closed | ✅ |
| T5c | Fraction of real turns that fit 32k | 59,325 deduped turns, 1,227 transcripts | **1.87%** | ✅ |
| T6 | Realised steady-state saving | Octopus's own multipliers @ Terra context | **$0** (T3 blocks the switch) | ✅ |
| T7a | Privacy / local-only gate exists | full config schema grep | **none** | ❌ (finding) |
| T7b | Local-preferred request stays local | small req, ollama + cloud both eligible | **went to cloud** | ❌ (finding) |
| T7c | Oversized request fails closed with cloud present | 40k req, ollama + cloud | **silently went to cloud** | ❌ (finding) |
| — | Octopus's own test suite | `GOWORK=off go test ./...` | 9/9 packages ok | ✅ |

❌ marks a defect or absent capability found, not a failure of the test.

## Environment

- Go 1.26.2 darwin/arm64; Ollama 0.30.10, model `qwen2.5:3b`
- Real-model arm: `claude-haiku-4-5@20251001-global` via `litellm-stg.aip.gov.sg`
  Anthropic-shaped `/v1/messages` (served through Vertex — `msg_vrtx_` ids)
- Spend: T1 only, 4 requests × ~4.8k input tokens on Haiku — a few cents

## Notes

- macOS builds of Octopus hard-code `~/.octopus/config.yaml` and launch a menubar app.
  Testing required a headless entrypoint (`scripts/octotest-main.go`, a copy of the
  non-darwin `cmd/octopus/main.go` wiring). **No `~/.octopus` was created** — verified.
- `ollama serve` was started for T5. Ollama.app re-spawns it; it may still be running.
- No test payload contained repo content — filler text only.
